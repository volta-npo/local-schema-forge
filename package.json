{
  "name": "@volta-oss/local-schema-forge",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "Generate structured data blocks for common local business pages without making students memorize schema.org.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "test:rust": "cargo test --manifest-path backend/rust/Cargo.toml",
    "test:backend": "npm run test:rust"
  },
  "keywords": [
    "digital presence",
    "digital-equity",
    "local-first",
    "oss",
    "v3-production",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/local-schema-forge.git"
  },
  "homepage": "https://volta-npo.github.io/local-schema-forge/",
  "bugs": {
    "url": "https://github.com/volta-npo/local-schema-forge/issues"
  },
  "author": "Volta OSS Division <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^24.0.0"
  }
}
